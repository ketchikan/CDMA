#pragma once

#include "tower.h"

// TODO
// My thought is that the easiest naive approach is to treat every message as if it should go to every available recipient. We can narrow it down later with a smarter approach like a routing table.
// For now this is acceptable for a simple implementation.

/**
@class Network

*/
class Network
{
private:
public:
};